<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keepfit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="KeepFit"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeepFit">
        <activity
            android:name=".calories.SitUpActivity"
            android:label="@string/title_activity_sit_up"
            android:theme="@style/Theme.KeepFit.NoActionBar"></activity>
        <activity android:name=".CreateWorkout" />
        <activity
            android:name=".ViewCommentsActivity"
            android:label="@string/title_activity_view_comments"
            android:theme="@style/Theme.KeepFit.NoActionBar" />
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".authapp.PublicProfile" />
        <activity android:name=".authapp.ProfileActivity" />
        <activity android:name=".authapp.FollowInfo" />
        <activity android:name=".MainActivity" />
        <activity android:name=".authapp.ForgotPassword" />
        <activity android:name=".authapp.ProfileActivityEdits" />
        <activity android:name=".authapp.ScrollingActivity" />
        <activity android:name=".calories.CalorieActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".VideoUploadActivity" />
        <activity android:name=".authapp.ViewVideos" /> <!-- android:name=".ui.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_login"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- android:name=".authapp.FirebaseMainActivity" -->
        <!-- android:name=".MainActivity" -->
        <!-- android:label="@string/app_name"> -->
        <!-- android:name="MainActivity" -->
        <!-- android:label="@string/app_name"> -->
        <activity
            android:name=".authapp.FirebaseMainActivity"
            android:label="KeepFit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LivestreamActivity" />
        <activity android:name=".StartLivestreamActivity" />
        <activity android:name=".authapp.RegisterUser" />
    </application>

</manifest>