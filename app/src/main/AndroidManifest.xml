<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keepfit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="KeepFit"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeepFit">
        <activity android:name=".SearchResultsActivity"></activity>
        <activity android:name=".authapp.PublicProfile"></activity>
        <activity android:name=".authapp.ProfileActivity"></activity> <!-- <activity -->
        <!--<activity android:name=".MainActivity" /> -->
        <activity android:name=".authapp.ForgotPassword" />
        <activity android:name=".authapp.ProfileActivityEdits" />
        <activity android:name=".authapp.ScrollingActivity" /> <!-- <activity -->
        <!-- android:name=".ui.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_login"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <!--            android:name=".authapp.FirebaseMainActivity"-->


        <activity

            android:name="MainActivity"
            android:label="@string/app_name">

      <!--
            android:name=".StartLivestreamActivity"
            android:label="KeepFit"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VideoActivity" />
        <activity android:name=".LivestreamActivity" />
        <activity android:name=".authapp.RegisterUser" />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>